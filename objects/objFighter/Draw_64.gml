/*
var x1 = x - 16;
var y1 = -z - sprite_height - 16;
var x2 = x1 + 32;
var y2 = y1 + 4;

var max_hp = 100;
var rate = (hp / max_hp) * 100;
draw_healthbar(x1, y1, x2, y2, rate, c_black, c_red, c_lime, 0, true, true)
draw_text(x1, y1, string(hp));
*/
